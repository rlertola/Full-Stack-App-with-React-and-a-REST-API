import React from 'react';

const UserSignOut = () => {
  // return (
  //   // This component doesn't render any visual elements, it signs out the user and redirects to courses.
  // );
};

export default UserSignOut;
